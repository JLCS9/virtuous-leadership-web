import TestTBP from '../TestTBP';

// El componente TestTBP ya trae su propio fondo (BEIGE) y padding.
// Lo renderizamos directamente; el header del Layout queda encima.
export default function TestTemperamento() {
  return <TestTBP />;
}
